# Monorepo env layout
# -------------------
# backend/.env  -> all server secrets (Supabase, JWT, Gemini, etc.)
# frontend/.env -> VITE_* only (optional in local dev)
#
# Copy and fill:
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
